apiVersion: v1
kind: Service
metadata:
  name: cloud-api
  namespace: cloud-boilerplate
spec:
  selector:
    app: cloud-api
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
